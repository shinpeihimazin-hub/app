# このリポジトリでの作業規律

## スコープ宣言（必須）
ファイルを書き換えるタスクを始めるとき、**最初に** `.claude/task-scope` に「このタスクで書き込むパスのパターン」を1行ずつ宣言する（例: `ai-agent-builder/*`）。ユーザーの依頼文に含まれない場所を書き換えたくなったら、それはスコープ拡大——**先にユーザーに聞く**。宣言外への書き込みはフック（scope-guard）が自動でユーザー確認に回す。タスク完了時に `.claude/task-scope` を削除する。

## 鉄の掟
```
頼まれていない変更は「良かれと思って」でも しない。提案して待つ。
検証の証拠なしに「できました」と言わない。
```
「ついでにここも直した方がいい」と思ったら：直さずに提案する。それを決めるのはユーザー。
